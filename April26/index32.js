var height = 10;
var baseLength = 4;
var perimeter = 2*(height + baseLength);
var area = height * baseLength;

console.log(`The height and baseLength of the rectangular are ${height},${baseLength}, so it\'s perimeter is ${perimeter} and it\'s area is ${area} `)
